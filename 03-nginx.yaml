- name: nginx installation
  hosts: frontend
  tasks:
    - name: installation of nginx
      ansible.builtin.package:
        name: nginx
        status: present
    - name: start nginx
      ansible.builtin.service:
        name: nginx
        status: started
        enabled: yes
         